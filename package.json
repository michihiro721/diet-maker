{
  "name": "diet-maker",
  "private": "true",
  "scripts": {
    "build": "esbuild app/javascript/application.js --bundle --outdir=app/assets/builds"
  },
  "dependencies": {
    "esbuild": "^0.14.0"
  }
}
